# Header
Contents
Use `Control + Shift + m` to toggle to `tab` key moving focus. Alternatively,& dropping, then `tab` use `esnextc` interactive element on the page to move to Attach files by dragging selecting or pasting them
